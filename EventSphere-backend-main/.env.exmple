# Server Configuration
PORT=8000
FRONTEND_URL=http://localhost:5173

# Database (we'll set this up next)
DATABASE_URL=""

# Redis (we'll set this up after database)
REDIS_HOST=
REDIS_PORT=
REDIS_USERNAME=
REDIS_PASSWORD=

# JWT Secrets (we'll generate these)
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# OTP Secret (we'll generate this)
OTP_SECRET=

# Email (optional for now)
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM="EventSphere <noreply@eventsphere.com>"
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465

# Cloudinary (optional for now)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=